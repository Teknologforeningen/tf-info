{
    "deno.enable": true,
    "deno.lint": true,
    "deno.unstable": true,
    "fmt": {
        "lineWidth": 120
    },
    "tasks": {
        "dev": "deno run --watch --allow-read --allow-env --allow-net main.ts",
        "scripts": "swc compile --config-file .swcrc --out-dir public scripts && deno cache --reload --lock=deno.lock main.ts && deno run --lock=deno.lock --cached-only --allow-env --allow-read --allow-net main.ts"

    },
    "imports": {
        "@std/http": "https://deno.land/std@0.223.0/http/mod.ts",
        "@std/dotenv": "https://deno.land/std@0.223.0/dotenv/load.ts",
        "eta": "https://deno.land/x/eta@v3.1.0/src/index.ts",
    }
}
